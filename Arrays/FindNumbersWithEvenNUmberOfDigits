class Solution:
    def findNumbers(self, nums: List[int]) -> int:
        count = 0
        for num in nums:
            # using modulus to differentiate even and odd lengths
            if len(str(num)) % 2 == 0:
                count += 1
        return count
